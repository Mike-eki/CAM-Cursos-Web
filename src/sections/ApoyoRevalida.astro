---
import CardLink from '../components/CardLink.astro'
import CTAButton from '../components/CTAButton.astro'
import Icon from '../components/Icon.astro'
import TestimoniesCarousel from '../components/TestimoniesCarousel.astro'
import TrustNumbers from '../components/TrustNumbers.astro'
import Wrapper from '../components/Wrapper.astro'
import CertificateIcon from '../icons/CertificateIcon.astro'
import HeartHearIcon from '../icons/HeartHearIcon.astro'
import programs from "../data/programs.json";

export interface Props {
    isModal?: boolean;
}

const { isModal = false } = Astro.props;

const testimonies = [
    {
        quote: "Muy buena experiencia, te ayudan en todo lo referido al cursado y también los cursos intensivos para preparar mesas te salvan la vida, son cortos, las clases son integradoras y participativas, lo que te ayuda un montón ya que todos los finales son orales. Lo súper recomiendo 10/10.",
        author: "Lorena Escobar",
        role: "Estudiante de medicina",
        img: "img/authors/lorena_escobar.png",
        alt: "Foto de Lorena Escobar"
    },
    {
        quote: "Excelente lugar y profes. ¡La atención un 10, súper recomendados!",
        author: "Priscila Gómez",
        role: "Estudiante de medicina",
        img: "img/authors/prisci_gomez.png",
        alt: "Foto de Priscila Gómez"
    }
];
---

<Wrapper className="grid-wrapper">

    <header class="heading-icon h-grid-one">
        <HeartHearIcon />
        <h2>¡Acelera tu carrera de medicina!</h2>
    </header>

    <section class="trust-container">
        <TrustNumbers num="600" title="Clases impartidas" />
        <TrustNumbers num="36" title="Medicos" />
        <TrustNumbers num="90" title="Simulacros" />
    </section>

    <section class="companies description-one">
        <p>Creemos firmemente que el acompañamiento es clave para el éxito de tu carrera académica.</p>
    </section>

    <section class="grid card-one">
        {programs.filter(p => p.courseId === 'clases-apoyo-medicina').map((program) => (
            <CardLink 
                title={program.title}
                path={program.path}
                desc={program.desc}
                className="rect"
                aspectRatio="16/7.7"
                courseId={program.courseId}>
                <Icon icon={program.icon} width="30" slot="icon"/>
            </CardLink>
        ))}
    </section>

    <header class="heading-icon h-grid-two">
        <CertificateIcon />
        <h2>¡No pierdas tu progreso!</h2>
    </header>

    <section class="companies description-two">
        <p>Solicita la reválida y mantén viva tu oportunidad de rendir el final. Gestiona tus trámites académicos  y mantenete en camino.</p>
    </section>

    <section class="grid card-two">
        {programs.filter(p => p.courseId === 'revalidacion-tramites').map((program) => (
            <CardLink 
                title={program.title} 
                path={program.path} 
                desc={program.desc}
                className="rect" 
                aspectRatio="16/7.7" 
                courseId={program.courseId}>
                <Icon icon={program.icon} width="30" slot="icon" />
            </CardLink>
        ))}
    </section>

    <section class="heading-icon h-grid-three">
        <h2>Experiencias de estudiantes</h2>
    </section>

    <section class="testimonies">
        <TestimoniesCarousel testimonies={testimonies} />
    </section>

    <footer class="button">
        <CTAButton 
        text="Quiero acelerar mi carrera de medicina"
        waMessage="¡Hola! Les escribo desde su web porque quiero acelerar mi carrera de medicina en CAM." />
    </footer>

</Wrapper>

<style>
    .heading-icon {
        text-align: center;
    }

    .trust-container {
        display: flex;
        justify-content: center;
        gap: 0.5rem;
        margin-bottom: 1rem;
    }

    .companies {
        display: flex;
        flex-direction: column;
        text-align: center;
        align-items: center;
        gap: 0.5rem;
    }
    .companies small {
        font-weight: 600;
    }
    .logos {
        display: flex;
        justify-content: center;
        gap: 1.25rem;
    }

    .row {
        display: flex;
        gap: 0.75rem;
    }

    .grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 0.75rem;
    }

    /* Rectangle cards span 2 columns by default */
    :global(.grid > .rect) {
        grid-column: span 2;
    }

    h2, p {
        margin: 0;
    }

    @media screen and (max-width: 400px) {
        .grid {
            grid-template-columns: 1fr;
        }
        /* On mobile, all cards take full width */
        :global(.grid > .rect) {
            grid-column: span 1;
        }
    }

    /* Tablet: cards fill 2 columns */
    @media screen and (min-width: 401px) and (max-width: 849px) {
        .grid {
            grid-template-columns: repeat(2, 1fr);
        }
        :global(.grid > .rect) {
            grid-column: span 2;
        }
    }

    @media screen and (min-width: 850px) {
        .grid {
            grid-template-columns: auto;
        }
        .mobile-wrapper {
            grid-template-columns: 1fr 1fr 1fr 1fr;
        }

        .h-grid-one {
            grid-column: 1/3;
            grid-row: 1;

            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;

            font-size: 1.5rem;
            font-weight: 500;
        }
        .h-grid-two {
            grid-column: 3/5;

            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;

            font-size: 1.5rem;
            font-weight: 500;
        }
        .h-grid-three {
            grid-column: 1/5;

            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;

            font-size: 1.5rem;

        }
        .trust-container {
            grid-column: 1/5;
            grid-row: 3;
        }
        .description-one {
            grid-column: 1/3;
            grid-row: 2;
        }
        .description-two {
            grid-column: 3/5;
        }
        .description-one p, .description-two p {
            max-width: 42ch;
            font-size: 1.5rem;
            margin-inline: auto;
        }
        .card-one {
            grid-column: 3/5;
            grid-row: 1/3;
        }
        .card-two {
            grid-column: 1/3;
            grid-row: 4/6;
        }
        .last-card {
            grid-column: 3;
            grid-row: 1;
        }
        .testimonies {
            grid-column: 2/4;
        }
        .button {
            grid-column: 2/4;
        }
    }

</style>
